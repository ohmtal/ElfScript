# raylib-ElfScript 0.8 

I added Raylib ElfScript to ElfScript addons. Will be merged with ElfFlux and then with raylib-elfscript when finished. 

- fixed issue when compiled with raylib 6.0 and compatible to current master (2026-09-09)
- updated main.elf to work without callbacks - so its closer to raylib. New defaultMain overwrites WindowShouldClose


